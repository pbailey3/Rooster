@model WebUI.DTOs.RoleDTO

@{
    ViewBag.Title = "Details";
}

<h2>Role Details</h2>
<div>

    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
           @Html.ActionLink("Edit", "RoleEdit", new { businessid = Model.BusinessId, id = Model.Id, businessLocationId = ViewBag.BusinessLocationId }, new { @class = "btn btn-primary" }) 
        </div>
    </div>
</div>
        <p>
            
            @Html.ActionLink("< Back to list", "RoleIndex", new { businessid = Model.BusinessId, id = Model.Id, businessLocationId = ViewBag.BusinessLocationId }, new { @class = "btn btn-primary" })
        </p>
